<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.boot.ev_charge.station.ChargerMapper">

    <insert id="insertCharger" parameterType="com.boot.ev_charge.station.ChargerDto">
        INSERT INTO charger (station_id, connector_type, power_kw, status)
        VALUES (#{stationId}, #{connectorType}, #{powerKw}, #{status})
    </insert>

</mapper>