<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.boot.ev_charge.user.UserMapper">

  <!-- 아이디로 회원 조회 -->
  <select id="findByLoginId"
          parameterType="String"
          resultType="com.boot.ev_charge.user.UserDto">

    SELECT id, login_id, password, name, email,
           phone, role, created_at, updated_at
    FROM user
    WHERE login_id = #{loginId}

  </select>

  <!-- 회원가입 -->
  <insert id="insert"
          parameterType="com.boot.ev_charge.user.UserDto"
          useGeneratedKeys="true"
          keyProperty="id">

    INSERT INTO user
    (login_id, password, name, email, phone, role)

    VALUES
    (#{loginId}, #{password}, #{name},
     #{email}, #{phone}, 'USER')

  </insert>

  <!-- 아이디 중복 -->
  <select id="countByLoginId"
          parameterType="String"
          resultType="int">

    SELECT COUNT(*)
    FROM user
    WHERE login_id = #{loginId}

  </select>

  <!-- 이메일 중복 -->
  <select id="countByEmail"
          parameterType="String"
          resultType="int">

    SELECT COUNT(*)
    FROM user
    WHERE email = #{email}

  </select>

</mapper>