<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.boot.ev_charge.notification.NotificationMapper">

    <!-- 1. 신규 알림 데이터 DB 적재 (💡 VALUES 소괄호 문법 완벽 수정 보정) -->
    <insert id="insertNotification" parameterType="com.boot.ev_charge.notification.NotificationDTO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO notification (user_id, type, title, content, is_read, reference_id, reference_type, created_at)
        VALUES (#{userId}, #{type}, #{title}, #{content}, COALESCE(#{isRead}, 'N'), #{referenceId}, #{referenceType}, CURRENT_TIMESTAMP)
    </insert>

    <!-- 2. 안 읽은 알림 목록 조회 (개수 검증용) -->
    <select id="selectUnreadListByUserId" resultType="com.boot.ev_charge.notification.NotificationDTO">
        SELECT id, user_id AS userId, type, title, content, is_read AS isRead, reference_id AS referenceId, reference_type AS referenceType, created_at AS createdAt
        FROM notification
        WHERE user_id = #{userId} AND is_read = 'N'
        ORDER BY created_at DESC
    </select>

    <!-- 3. 전체 알림 목록 최신순 조회 (알림센터 목록용) -->
    <select id="selectAllListByUserId" resultType="com.boot.ev_charge.notification.NotificationDTO">
        SELECT id, user_id AS userId, type, title, content, is_read AS isRead, reference_id AS referenceId, reference_type AS referenceType, created_at AS createdAt
        FROM notification
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
    </select>

    <!-- 4. 단일 알림 읽음 처리 (N -> Y) -->
    <update id="updateNotificationReadStatus">
        UPDATE notification SET is_read = 'Y' WHERE id = #{id}
    </update>

    <!-- 5. [교정 완료] 전체 알림 일괄 읽음 처리 (인터페이스 N -> Y 명세 복원 및 동기화) -->
    <update id="updateAllNotificationReadStatus">
        UPDATE notification SET is_read = 'Y' WHERE user_id = #{userId}
    </update>

    <!-- ========================================================================= -->
    <!-- 🚨 [신설] 3대 알림 라이프사이클 완전 파기 및 자동 전 스위칭 삭제 전용 쿼리 컴포넌트 -->
    <!-- ========================================================================= -->

     <!-- ========================================================================= -->
    <!-- 🚨 [최종 마감] 3대 알림 라이프사이클 완전 파기 및 자동 전 스위칭 삭제 전용 쿼리 -->
    <!-- ========================================================================= -->

    <!-- [시점 1, 4 및 공통]: 사용자가 알림창에서 카드 클릭 시, DB notification 테이블에서 해당 데이터를 즉시 완전 영구 삭제 -->
    <delete id="deleteNotificationById" parameterType="long">
        DELETE FROM notification 
        WHERE id = #{id}
    </delete>

    <!-- [시점 2]: 예약이 충전중(CHARGING) 상태로 진입 시, 해당 예약 번호에 남아있던 과거 '15분 전 입고 알림'을 자동 영구 삭제 -->
    <delete id="deleteBeforeNoticeByResId" parameterType="long">
        DELETE FROM notification 
        WHERE reference_type = 'RESERVATION' 
          AND type = 'RESERVATION_BEFORE' 
          AND reference_id = #{referenceId}
    </delete>

    <!-- 시간이 다 되었으므로 화면에 대기 중이던 기존 '⚡ 충전 진행 중' 알림을 DB 테이블에서 "자동으로 즉시 영구 삭제" -->
    <delete id="deleteStartNoticeByResId" parameterType="long">
        DELETE FROM notification 
        WHERE reference_type = 'CHARGE' 
          AND type = 'CHARGE_START' 
          AND reference_id = #{referenceId}
    </delete>

</mapper>